Track C COMPLETE: 91f625a

Track C (Jobs) shipped a full job module: kanban board with drag-drop (`@dnd-kit/core` + `@dnd-kit/sortable`), a list view at `/jobs/list`, create/detail/edit pages, and soft-delete. Status transitions run through `changeJobStatusAction`, which loads the current row, updates status (setting `started_at` / `completed_at` as appropriate), and writes a `worklog_entries` row with `entry_type='system'`, `related_type='job'`, and a human-readable body. Covered by a 19-case unit test, an integration test that verifies the worklog row is written, and a Playwright E2E that signs up a tenant, seeds a customer via admin, and walks create → status change → board → delete. `typecheck`, `lint`, `test` (60 passing), `build`, and `test:e2e` (6 passing) all green.
