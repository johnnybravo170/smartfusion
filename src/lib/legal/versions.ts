/**
 * Current versions of the public legal documents users must accept at signup.
 *
 * Bumping a version here will trigger a re-prompt on next sign-in once that
 * flow is wired (out of scope for the initial signup-acceptance card). Use
 * the YYYY-MM-DD format and keep it in sync with the `Last updated` line on
 * the corresponding /terms or /privacy page.
 */

export const CURRENT_TOS_VERSION = '2026-05-09';
export const CURRENT_PRIVACY_VERSION = '2026-04-29';
